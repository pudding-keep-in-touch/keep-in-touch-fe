export default function ModalPage() {
  return <></>
}
// 없으면 Application error: a client-side exception has occurred (see the browser console for more information).
// 코드 작성 시 화면에 그려지기 때문에 비워두었다.
